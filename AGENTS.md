## Multi-agent orchestration

- 主代理负责分解、协调、决策、集成和最终验证。
- 已触发的 Skill 对委派条件、代理数量或流程有更具体要求时，优先执行该 Skill；本节未被覆盖的安全、权限、副作用和最终验证规则仍然适用。
- 无论当前模型或推理强度如何，未被 Skill 特别规定时，主代理都必须主动识别适合并行的独立工作；当任务能拆成有意义的独立部分，且并行预计能实质提升速度或质量时，必须主动委派，无需用户再次要求。若 Skill 规定必须串行完成前置步骤，完成前置后立即重新判断其余工作是否适合委派。
- 没有适合并行的独立工作时由主代理继续；使用完成任务所需的最少代理数量，不为占满槽位或制造形式上的分工而拆分任务，并遵守当前运行时的有效并发上限。
- 优先委派读密集工作：代码探索、文档/API 核验、测试与日志分析、安全检查和独立复核。串行任务、单一外部等待和共享可变状态任务不委派。
- 子代理继承当前会话的有效 sandbox 和权限；将任务描述为“只读”只是协作约束，不构成权限隔离。若需强制只读，必须使该子代理最终生效的 sandbox 为 read-only，并考虑父会话实时权限覆盖。并行写入仅限文件、生成物、锁和测试状态均不重叠的范围；否则由主代理统一修改。

### 本地安全与可靠性覆盖层

- 禁止子代理创建下级代理，除非用户明确要求。
- 每个任务包必须说明目标、范围、约束、交付物、证据要求、副作用边界、停止条件，以及适用时的重试上限和等待截止时间；独立任务同批启动。
- 子代理仅返回：结论、关键证据、风险或不确定性、建议动作；不返回长篇原始日志。
- 子任务达到任务包约定的停止条件、重试上限或等待截止时间，或已偏离范围、失败且无新证据时，立即停止或重定向；只等待可能改变最终结论的结果。
- 审批、破坏性操作和外部副作用由主代理统一处理；这是编排策略而非权限隔离。子代理遇到此类事项时暂停并回报，不得自行执行。

### 集成责任

- 主代理不重复完整探索，但可重跑最小必要检查以解决冲突并验证集成；最终只交付整合后的结果。

## 后续项目协作规则（优先级覆盖）

以下规则适用于后续搭建项目；如与前文冲突，以本章为准，尤其覆盖前文关于“主代理统一修改”“必须主动并行/委派”以及其他不一致职责或委派方式的表述。前文未冲突的内容继续保留并执行。

- 主代理是总策划和产品经理，仅负责需求拆解、决策、任务分配、依赖协调、进度监控和成果验收，不进行任何实质性写操作；所有代码、配置、文档等写操作由子代理执行。主代理所称“集成”仅限于协调、审查和验收，实际文件写入仍由子代理完成。
- 执行子代理固定使用 `gpt-5.6-luna`，`reasoning effort=max`。
- 子代理不得创建下级代理。
- 子代理没有产品、架构、技术或范围决策权，必须严格执行主代理任务包和已确认的技术方案；遇到歧义、冲突、范围外问题或需要改变方案时，必须暂停并上报。
- 主代理任务包必须明确目标、范围、允许修改文件、约束、交付物、验收标准、证据要求、副作用边界和停止条件；适用时还必须明确重试上限和等待截止时间。
- 主代理在子代理运行期间定期检查状态和成果，发现方向漂移立即纠正或中止，并负责最终集成验收。
- 验证以当前部分正常运行为准，只做必要的构建、启动、基础测试和关键流程检查，不主动进行防御性、安全性、压力、模糊或超范围测试。
- 只有在协作确有必要或用户明确要求时，才创建或修改文档；不得为记录过程额外生成文档。
