# 个人技术网站技术方案

> 状态：已确认  
> 确认日期：2026-09-19  
> 产品基线：[产品方案.md](./产品方案.md)  
> 领域词汇：[CONTEXT.md](./CONTEXT.md)  
> 原型基线：[design/v2/README.md](./design/v2/README.md)  
> 架构决策：[docs/adr/](./docs/adr/)

## 1. 方案目标

本方案用于实现一个以中文内容为主的个人技术网站，并将其部署到现有的异地物理服务器。首版必须形成以下完整闭环：

1. 公开站在无内容时可以正常访问。
2. 唯一管理员可以登录后台。
3. 管理员可以创建、发布、修改和撤回文章。
4. 管理员可以创建、公开、隐藏和排序项目。
5. 文章、项目和图片保存失败时不会破坏上一份公开版本。
6. 会话过期后，当前未保存输入仍留在页面中。
7. 部署、回退、备份和恢复步骤可以使用脱敏配置复现。

首版不是内容平台、微服务系统或 Kubernetes 项目。后续可以把同一镜像迁移到 k3s 作为独立实验，但不能让该实验阻塞网站上线。

## 2. 技术基线

| 范围 | 选择 |
| --- | --- |
| 构建运行时 | Go 1.27.1、Node.js 24.16.0、npm 11.13.0；实现时固定补丁版本 |
| 后端 | Go 模块化单体，标准库 `net/http` 与 `ServeMux` |
| 公开站 | Go `html/template` 服务端渲染 |
| 管理端 | Vue 3、TypeScript、Vite、Vue Router |
| API | 同源 REST JSON API，统一前缀 `/api/v1/` |
| API 契约 | OpenAPI；`openapi-typescript` 生成 TypeScript 类型 |
| 数据库 | 1Panel 现有 MySQL Community 8.4.11 |
| 数据访问 | 显式 SQL、`sqlc`、`goose` |
| 认证 | 服务端会话、Cookie、CSRF Token、Argon2id |
| Markdown | 服务端解析、过滤、高亮并保存派生结果 |
| 图片 | 宿主机持久目录；解码、去元数据、重新编码 |
| 公网入口 | 1Panel v1.10.34-lts 管理的 OpenResty |
| 生产编排 | Docker Compose，不包含 MySQL 与第二套反向代理 |
| 发布 | Git Tag，服务器多阶段构建，人工触发部署 |
| 备份 | 项目生成 MySQL 与图片统一批次，1Panel 上传到阿里云 OSS |

依赖必须通过 `go.mod/go.sum`、`package-lock.json`、Dockerfile 基础镜像版本与摘要锁定。不得使用 `latest` 作为生产镜像版本。

## 3. 总体架构

```text
Internet
   │
   ▼
1Panel OpenResty :80/:443
   ├── /admin/* ───────────────► admin-web :8080
   ├── /api/v1/* ──────────────► app :8080
   ├── /media/* ───────────────► app :8080
   └── 其他公开页面 ───────────► app :8080
                                      │
                                      ├── MySQL 8.4.11
                                      ├── /data/uploads
                                      └── GitHub 公共 API

1Panel 计划任务
   ├── MySQL 逻辑备份 ─────────► 阿里云 OSS（远程备份）
   └── /data/uploads 归档 ─────► 阿里云 OSS（远程备份）
```

### 3.1 组件职责

**OpenResty**

- 终止 TLS、处理 HTTP 到 HTTPS 跳转。
- 按路径把请求转发给公开应用或管理端静态容器。
- 设置公网请求大小、基础限速和安全响应头。
- 不直接读取上传图片目录，不判断图片是否可公开。

**app**

- 服务端渲染全部公开页面。
- 提供 `/api/v1/` 管理 API。
- 处理管理员认证、会话、CSRF 和业务校验。
- 解析 Markdown，生成安全 HTML、目录、文章预览和代码高亮。
- 处理图片上传、访问控制和公开读取。
- 访问 MySQL 与 GitHub 公共 API。

**admin-web**

- 只提供 `/admin/` 下的 Vue SPA 与静态资源。
- 使用同源 `/api/v1/`，不启用 CORS。
- 保存未提交的表单状态，处理字段错误、会话恢复和版本冲突。

**MySQL**

- 保存文章、项目、管理员、会话、图片元数据和内容引用关系。
- 由 1Panel 现有 MySQL 8.4.11 提供，不纳入项目 Compose。
- 不承诺兼容 MariaDB。

**图片持久目录**

- 建议宿主机路径为 `/opt/mywebsite/data/uploads`。
- 通过 bind mount 挂入 `app` 容器。
- 只有 `app` 负责读写和访问判断。

## 4. 仓库结构

```text
MyWebsite/
├── cmd/
│   ├── server/                 # HTTP 服务入口
│   └── adminctl/               # 创建管理员、重置密码、维护命令
├── internal/
│   ├── app/                    # 装配、生命周期和配置
│   ├── auth/                   # 密码、会话、CSRF
│   ├── article/                # 文章领域与用例
│   ├── project/                # 项目领域与排序
│   ├── media/                  # 上传、转换、引用与访问控制
│   ├── markdown/               # Markdown 派生管线
│   ├── publicsite/             # 公开页面处理器和视图模型
│   ├── adminapi/               # 管理 API 处理器
│   ├── database/               # 事务、sqlc 适配和健康检查
│   └── platform/               # 日志、时间、请求 ID、GitHub 客户端
├── web/
│   ├── public/
│   │   ├── templates/          # html/template 布局、页面和局部模板
│   │   └── assets/             # 公开 CSS、JS、默认图片
│   └── admin/
│       ├── src/                # Vue SPA
│       ├── tests/
│       ├── package.json
│       └── package-lock.json
├── api/
│   └── openapi.yaml            # REST 契约源
├── db/
│   ├── migrations/             # goose SQL 迁移
│   ├── queries/                # sqlc 查询
│   └── sqlc.yaml
├── deploy/
│   ├── compose.yaml
│   ├── compose.candidate.yaml  # 正式切换前的本机候选实例
│   ├── openresty/              # 脱敏代理片段与说明
│   ├── scripts/                # 预检、发布、回退、恢复验证
│   ├── env.example
│   └── site.example.yaml       # 脱敏站点资料样例
├── tests/
│   └── e2e/                    # Playwright 关键路径
├── docs/adr/
├── CONTEXT.md
├── 产品框架.md
├── 产品方案.md
├── 技术方案.md
├── go.mod
├── go.sum
└── Makefile                    # 跨平台入口可由等价脚本补充
```

现有 `design/v2/` 保留为视觉与交互验收基线，不作为运行时代码直接引用。旧的 `design/frontend/`、`design/backend/` 不得成为需求来源。

## 5. 后端分层与依赖方向

每个业务模块按以下方向组织：

```text
HTTP handler → application service → repository interface
                                      │
                                      ▼
                                MySQL implementation
```

规则如下：

- HTTP 层负责解析请求、认证上下文、状态码和 Problem Details，不包含事务流程。
- Application service 负责发布、撤回、公开、隐藏、排序和会话恢复等用例。
- Repository 使用 `sqlc` 生成代码，但生成类型不直接暴露给 HTTP 层。
- 跨多个写操作的用例由 application service 显式开启事务。
- 公开页面处理器调用相同的 application/query service，不通过 HTTP 回调自己的 API。
- 时间来源封装为 `Clock`，测试可以固定 `Asia/Shanghai` 日期边界。

## 6. 数据模型

所有时间戳在 MySQL 中保存为 UTC 的 `DATETIME(6)`；需要显示“站点日期”时转换为 `Asia/Shanghai`，格式固定为 `YYYY-MM-DD`。数据库连接显式设置 UTC，不依赖宿主机默认时区。数据库与连接统一使用 `utf8mb4`；首版固定 MySQL 8.4 的 `utf8mb4_0900_ai_ci` 排序规则，迁移和集成测试必须验证该配置。

### 6.1 `admins`

| 字段 | 约束 |
| --- | --- |
| `id` | `BIGINT UNSIGNED` 主键 |
| `username` | 3–64 个 ASCII 字符，唯一，规范化后比较 |
| `password_hash` | Argon2id 编码串 |
| `created_at` | UTC |
| `updated_at` | UTC |

首版只允许存在一个启用管理员。没有公开注册、在线找回密码或后台管理员管理页面。

### 6.2 `admin_sessions`

| 字段 | 约束 |
| --- | --- |
| `id` | 内部主键 |
| `token_hash` | 会话 Token 的 SHA-256，唯一 |
| `admin_id` | 外键到 `admins` |
| `csrf_hash` | CSRF Token 的哈希 |
| `last_seen_at` | 用于 8 小时空闲过期 |
| `absolute_expires_at` | 创建后最多 24 小时 |
| `revoked_at` | 主动退出、密码重置或吊销时设置 |
| `created_at` | UTC |

退出登录吊销当前会话。密码重置吊销该管理员的全部会话。重新认证成功后轮换会话与 CSRF Token，防止会话固定。

### 6.3 `articles`

| 字段 | 约束 |
| --- | --- |
| `id` | `BIGINT UNSIGNED` 内部主键 |
| `public_ulid` | 首次发布时生成，唯一、不可修改，可为空 |
| `title` | 必填，1–200 个 Unicode 字符 |
| `body_markdown` | 原始 Markdown；草稿可为空，最大 2 MiB |
| `body_html` | 安全派生 HTML |
| `toc_json` | 二、三级标题目录 |
| `preview_text` | 自动生成的正文预览 |
| `renderer_version` | 派生管线版本 |
| `status` | `draft` 或 `published` |
| `first_published_at` | 第一次发布时设置，之后不变 |
| `version` | 乐观锁版本，从 1 开始递增 |
| `created_at` | UTC |
| `updated_at` | UTC |

约束：

- `published` 必须具有非空正文、`public_ulid` 和 `first_published_at`。
- `public_ulid` 一旦生成不允许更改或复用。
- 撤回只改变状态，不清空公开地址或首次发布日期。
- 公开查询只返回 `published`。
- 文章列表固定按 `first_published_at DESC, id DESC` 排序，避免同一时间或同一站点日期的内容发生翻页漂移。

### 6.4 `projects`

| 字段 | 约束 |
| --- | --- |
| `id` | `BIGINT UNSIGNED` 主键 |
| `name` | 必填，1–120 个 Unicode 字符 |
| `github_url` | 隐藏项目可为空；公开项目必须有效，最大 2048 字节 |
| `image_asset_id` | 可为空，外键到 `media_assets` |
| `status` | `hidden` 或 `public` |
| `sort_order` | 公开项目必填且唯一；隐藏项目为空 |
| `version` | 乐观锁版本 |
| `created_at` | UTC |
| `updated_at` | UTC |

公开排序保存必须在一个事务内完成。请求提交完整的公开项目 ID 顺序，服务端验证集合无重复、无缺失且全部是当前公开项目，再统一更新顺序。任何从 `hidden` 变为 `public` 的项目都追加到末尾，不恢复隐藏前的位置。公开查询固定按 `sort_order ASC, id ASC` 排序。

### 6.5 `media_assets`

| 字段 | 约束 |
| --- | --- |
| `id` | ULID，作为不可猜测资源标识 |
| `storage_key` | 相对路径，唯一 |
| `source_media_type` | 上传时检测到的真实类型 |
| `stored_media_type` | 重新编码后的类型 |
| `byte_size` | 输出文件大小 |
| `width` / `height` | 输出像素尺寸 |
| `sha256` | 输出文件哈希 |
| `created_by` | 管理员 ID |
| `created_at` | UTC |

不以原始文件名作为公开路径，不保存原始文件本体。首版不自动删除孤儿资源。

### 6.6 `article_media`

保存文章与图片之间的引用关系，包括 `article_id`、`asset_id` 和最长 300 个 Unicode 字符的 `alt_text`。每次保存文章时由 Markdown 解析器重新计算并在文章事务中整体替换。

访客访问图片时必须满足以下任一条件：

- 图片被一篇 `published` 文章引用；
- 图片是一个 `public` 项目的当前展示图。

管理员登录后可以预览尚未公开的图片。内容下线后文件仍保留，但访客请求立即返回 404。

### 6.7 `project_order_state`

保存一行项目排序集合状态，包含固定主键、`version` 和 `updated_at`。项目管理列表同时返回该集合版本；发布、隐藏和全量重排都必须在事务内先使用 `SELECT ... FOR UPDATE` 锁定这一行，再修改项目并递增集合版本。它用于防止两个标签页分别排序或公开项目时发生 `MAX(sort_order)+1` 竞争。

## 7. 业务事务

### 7.1 保存文章

1. 接收完整的标题、Markdown 和当前 `version`，检查会话与 CSRF。
2. 校验标题；发布操作额外校验正文非空。
3. 解析 Markdown，校验图片引用和替代文字。
4. 生成安全 HTML、目录、预览和引用集合。
5. 开启 MySQL 事务。
6. 使用 `WHERE id = ? AND version = ?` 更新文章。
7. 更新 `article_media`。
8. 提交事务；受影响行数为零时回滚并返回 `409 Conflict`。

第一次发布还必须在同一事务中生成 ULID 和首次发布时间。派生数据或引用更新任何一步失败，都不能改变上一份公开版本。

“保存”与“发布”是两个不同意图，但发布请求本身携带当前完整编辑状态，不能要求前端先调用保存再调用发布。发布端点在一个事务中同时保存字段、派生数据、图片引用和状态；因此发布失败时，数据库仍保持提交前的完整草稿或公开版本。已发布文章的普通保存也携带完整编辑状态，并在一个事务内直接更新公开版本。

客户端在请求提交成功后丢失响应并重试时，旧 `version` 会得到 `409 Conflict`。管理端应重新读取服务器版本；如果服务器内容已经与本地提交一致，则提示此前保存已成功，否则进入人工合并流程，不能把它降级为普通保存失败后盲目覆盖。

### 7.2 撤回文章

- 要求管理员明确确认后发送请求。
- 使用版本号更新为 `draft`。
- 不删除 Markdown、派生数据、ULID、首次发布日期或图片。
- 事务提交后公开页面和相关图片访问立即按新状态判定。

### 7.3 保存与公开项目

- 隐藏项目只要求名称。
- 公开请求携带当前完整项目表单、项目 `version` 和排序集合 `order_version`，不要求前端先保存再公开。
- 公开项目前校验名称、GitHub URL 格式，并调用 GitHub 公共 API 确认仓库存在且公开。
- GitHub 超时、限流或服务故障返回可重试错误，不把“暂时无法验证”误报为“仓库不存在”。
- 事务先锁定 `project_order_state` 和涉及的项目；公开时使用锁内计算的最大顺序追加到末尾，并递增集合版本。
- 重新公开已经隐藏的项目同样追加到末尾。
- 隐藏公开项目前需要确认，隐藏后将 `sort_order` 设为空。

全量重排请求携带 `order_version` 和完整公开项目 ID 数组。事务锁定排序状态和全部公开项目，验证集合无重复、无缺失后，先把当前顺序整体移动到不会与最终正整数区间冲突的临时区间，再使用单条 `CASE` 更新为 `1..N`，最后递增集合版本并提交。集合版本不一致返回 `409 Conflict`。

## 8. HTTP 路由

### 8.1 公开页面

| 方法 | 路径 | 行为 |
| --- | --- | --- |
| `GET` | `/` | 首页，最多 3 篇文章和 3 个项目 |
| `GET` | `/articles` | 每页 10 篇；非法或越界页码返回 404 |
| `GET` | `/articles/{ulid}` | 只显示已发布文章 |
| `GET` | `/projects` | 按人工顺序显示全部公开项目 |
| `GET` | `/about` | 资料与内容统计 |
| `GET` | `/media/{assetID}` | 动态判断公开引用或管理员身份 |
| `GET` | `/-/live` | 进程存活，不检查外部依赖 |
| `GET` | `/-/ready` | 检查数据库、Schema 版本和必要目录 |

草稿、撤回文章、隐藏项目资源和不存在内容统一返回正确的 404 页面。内部错误返回临时故障页面，不显示堆栈或内部信息。

文章为零时只有 `page=1` 显示空状态；`page=0`、负数、非整数和 `page>1` 均返回 404。标题搜索由管理 API 完成，使用参数化 SQL，并对 `%`、`_` 等通配语义做明确转义；大小写匹配遵循固定数据库排序规则。

所有公开页面在首个 HTML 响应中输出独立的 `<title>` 和 description。文章页使用标题与自动预览生成 Open Graph 信息；默认分享图片、默认头像和默认项目图作为嵌入静态资源发布。当前年份按 `Asia/Shanghai` 计算。关于页的文章数和项目数分别只统计已发布文章与公开项目。

### 8.2 管理 API

认证：

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `POST` | `/api/v1/session` | 用户名和密码登录 |
| `GET` | `/api/v1/session` | 当前会话和 CSRF Token |
| `POST` | `/api/v1/session/reauth` | 同页重新认证并轮换会话 |
| `DELETE` | `/api/v1/session` | 退出登录 |

文章：

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `GET` | `/api/v1/articles` | 标题搜索、状态筛选、每页 20 篇 |
| `POST` | `/api/v1/articles` | 创建草稿 |
| `GET` | `/api/v1/articles/{id}` | 读取编辑数据 |
| `PUT` | `/api/v1/articles/{id}` | 保存完整编辑状态 |
| `POST` | `/api/v1/articles/{id}/publish` | 携带完整编辑状态并原子发布 |
| `POST` | `/api/v1/articles/{id}/withdraw` | 撤回 |

项目：

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `GET` | `/api/v1/projects` | 返回公开组和隐藏组 |
| `POST` | `/api/v1/projects` | 创建隐藏项目 |
| `GET` | `/api/v1/projects/{id}` | 读取编辑数据 |
| `PUT` | `/api/v1/projects/{id}` | 保存完整编辑状态 |
| `POST` | `/api/v1/projects/{id}/publish` | 携带完整表单并原子公开 |
| `POST` | `/api/v1/projects/{id}/hide` | 隐藏 |
| `PUT` | `/api/v1/projects/order` | 携带集合版本，原子保存全部公开项目顺序 |

图片：

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `POST` | `/api/v1/media` | 上传并返回资源 ID、预览地址和 Markdown 引用 |
| `GET` | `/api/v1/media/{id}` | 管理员预览未公开图片 |

首版不提供删除文章、删除项目、删除图片、文件列表或公开写 API。

### 8.3 并发控制

文章和项目写请求携带记录 `version`；公开项目集合操作还携带 `order_version`。服务端发现任一版本不一致时返回 `409 Conflict`，响应包含服务器当前版本号，但不返回或覆盖管理员当前输入。管理端提示内容已被其他标签页更新，由管理员刷新或复制当前输入后人工合并。

### 8.4 错误格式

API 使用 `application/problem+json`：

```json
{
  "type": "https://example.invalid/problems/validation",
  "title": "请求内容不符合要求",
  "status": 422,
  "code": "validation_failed",
  "request_id": "01J...",
  "errors": {
    "body_markdown": ["发布文章时正文不能为空"]
  }
}
```

稳定错误码至少包括：

- `validation_failed`
- `authentication_required`
- `session_expired`
- `csrf_failed`
- `version_conflict`
- `github_repository_invalid`
- `github_verification_unavailable`
- `upload_too_large`
- `upload_type_invalid`
- `image_dimensions_exceeded`
- `save_failed`

HTTP 状态约定：语法错误使用 `400`，未登录或会话过期使用 `401`，已登录但权限或 CSRF 不通过使用 `403`，资源不存在使用 `404`，版本冲突使用 `409`，请求体过大使用 `413`，媒体类型不支持使用 `415`，字段或业务校验失败使用 `422`，限速使用 `429`，内部错误使用 `500`，数据库或必要依赖暂时不可用使用 `503`。

生产响应不得包含 SQL、文件路径、堆栈或凭据。

## 9. Markdown 管线

使用 Goldmark 及其 GFM 扩展解析 Markdown，使用 Chroma 生成代码高亮，并使用 Bluemonday 的定制白名单做最终 HTML 过滤。具体版本在 `go.mod` 中固定。

保存时按以下顺序执行：

1. 限制正文总字节数，避免过大请求占用内存。
2. 解析 Markdown AST，不启用原始 HTML 渲染。
3. 校验本地图片引用存在，并要求文章图片具有非空替代文字。
4. 为二、三级标题生成稳定且单篇唯一的锚点；锚点以标题规范化文本为基础，空标题或无法规范化的标题使用确定性短哈希，重复标题按出现顺序追加 `-2`、`-3`。
5. 生成目录 JSON；没有二、三级标题时保存空目录。
6. 忽略标题、图片和代码块，从其余正文生成约 160 个字符的预览。
7. 根据围栏语言生成代码高亮；未知语言按纯文本处理。
8. 输出 HTML 并进行白名单过滤。
9. 保存 Markdown、HTML、目录、预览、渲染器版本和图片引用。

正文链接只允许 `http`、`https` 和 `mailto` 等明确白名单协议，拒绝 `javascript:`、`data:` 等危险协议。首版文章图片只允许引用本站上传后返回的媒体 ID，不允许任意外链图片；管理员即使手改 Markdown，保存与发布时仍会重新解析并校验替代文字与引用。

启用的正文能力固定为标题、段落、列表、链接、本站图片、引用、表格、任务列表、删除线和围栏代码块，不开放运行时功能开关。

提供管理 CLI 重新生成全部文章派生数据。重建命令先显示计划与数量，按事务逐篇更新；任何失败必须可定位并允许重试。

## 10. 图片管线

### 10.1 上传校验

- OpenResty 的请求体限制略高于 5 MB，以容纳 multipart 开销。
- Go 使用 `http.MaxBytesReader` 设置最终请求上限。
- 允许 JPEG、PNG、WebP；拒绝 GIF、SVG 和其他格式。
- 不信任扩展名或浏览器 MIME，必须读取文件头并真实解码。
- 最长边不超过 8000 像素，总像素不超过 2000 万。
- 临时文件写入专用临时目录，不直接写最终路径。

### 10.2 规范化

- JPEG 解码后重新编码为 JPEG。
- PNG 解码后重新编码为 PNG。
- WebP 解码后，有透明通道输出 PNG，否则输出 JPEG。
- 动画 WebP 拒绝上传，不截取静态帧冒充完整内容。
- 重新编码移除 EXIF、拍摄位置、设备信息和其他原始元数据。
- JPEG 在去除元数据前先应用 EXIF 方向，避免重新编码后旋转错误。
- 最终键建议为 `YYYY/MM/{assetULID}.{ext}`。
- 写入完成后计算 SHA-256，并使用同一文件系统内的原子重命名进入最终位置。

数据库记录失败时删除本次刚生成但尚未登记的文件；这属于失败补偿，不属于产品所禁止的自动清理。已经成功登记的孤儿文件首版不自动删除。

上传和内容保存是两个独立请求，无法构成一个数据库事务。上传成功但文章或项目保存失败时会留下仅管理员可见的孤儿资源，这是已接受行为。提供 `adminctl media verify` 只读检查命令，用于报告数据库缺失文件、磁盘孤儿文件、尺寸与哈希不符，不自动修复或删除。

### 10.3 缓存

公开图片的可访问性会随文章撤回或项目隐藏而改变，因此响应使用 ETag 与 `Cache-Control: no-cache`，要求缓存重新验证；不得设置长期 immutable。嵌入 Go 镜像、文件名带哈希的公开静态资源可以使用一年 immutable 缓存。

## 11. 管理员认证与会话

### 11.1 管理员 CLI

提供以下命令：

```text
adminctl admin create
adminctl admin reset-password
adminctl content rebuild
adminctl media verify
```

命令在交互式终端读取密码，不接受明文密码命令行参数，不把密码写入 Shell 历史。管理员密码要求 12–128 个 Unicode 字符。Argon2id 参数以目标服务器实测约 100–500 ms 为目标，初始基线可使用 64 MiB 内存、3 次迭代、2 线程、16 字节盐和 32 字节输出。

### 11.2 Cookie 与 CSRF

- 会话 Token 使用至少 32 字节加密安全随机数；数据库只保存哈希。
- 生产 Cookie 使用 `__Host-` 前缀、`Secure`、`HttpOnly`、`SameSite=Strict`、`Path=/`，不设置 `Domain`。
- 本地 HTTP 开发使用单独配置，禁止把不安全 Cookie 配置带入生产。
- 登录后由会话接口返回 CSRF Token；修改请求放入 `X-CSRF-Token`。
- 服务端对保存、上传、发布、撤回、排序和退出登录同时校验 CSRF、`Origin` 和同源关系。
- 密码输入设置长度上限，防止超长输入放大 Argon2 计算成本；密码哈希参数升级时在成功登录后重新哈希。

### 11.3 会话过期恢复

API 返回 `401` 和 `session_expired` 后，Vue 在当前页面上打开重新登录对话框，不跳转、不销毁编辑组件。认证成功后刷新会话和 CSRF Token，关闭对话框；管理员再次点击保存，不自动重放失败请求。

多个并发请求同时过期时只允许出现一个重新认证流程，其余请求停止重试并等待新会话。登录失败或取消对话框都不得清空正文、光标位置、图片替代文字、项目字段或 dirty state。页面刷新、浏览器崩溃和设备重启不属于首版恢复保证；首版没有自动保存。

### 11.4 限速

- OpenResty 对公网路径设置基础请求速率和突发容量。
- Go 对登录失败按 IP 与用户名组合限速，并使用统一错误文案。
- 上传和 GitHub 验证设置更低的每会话速率。
- 限速状态不要求跨实例共享，因为首版只有一个应用实例。
- Go 只信任来自本机 OpenResty 的代理头，不直接信任任意客户端提供的 `X-Forwarded-For`。

## 12. 管理端实现

管理端保留 V2 原型的黑白灰、直角、细边框和无阴影视觉。使用现有 CSS Token 与语义化 Vue 组件，不引入 Tailwind、Element Plus、Pinia或通用请求缓存库。

建议路由：

```text
/admin/login
/admin/articles
/admin/articles/new
/admin/articles/:id
/admin/projects
/admin/projects/new
/admin/projects/:id
```

实现要求：

- Vue Router 使用 `/admin/` history base，静态容器对未知 `/admin/*` 回退到 `index.html`。
- 登录成功后默认进入 `/admin/articles`；没有独立 Dashboard。
- 一个轻量 session composable 保存会话状态和 CSRF Token。
- OpenAPI 生成类型，维护一个薄的 `fetch` 包装器处理 Problem Details。
- 编辑组件持有未保存表单状态；不自动保存到服务器或浏览器永久存储。
- 路由离开和窗口关闭时检测 dirty state 并确认。
- 在存在未保存修改时点击退出，先触发统一的离页保护；确认离开后直接退出，不再出现第二个“是否退出”确认。
- 上传进度使用 `XMLHttpRequest` 或支持上传进度的等价封装；其他请求使用 `fetch`。
- 项目排序同时支持拖拽和键盘操作，排序保存前不调用 API。
- 主题只跟随 `prefers-color-scheme`，不保存手动主题设置。
- 不实现草稿预览；管理员发布后通过公开文章地址检查最终排版。
- 项目自定义图片的替代文字使用当前项目名称；统一默认项目图作为装饰图输出空替代文字。

## 13. 配置管理

仓库提交 `deploy/env.example`，不得包含真实域名、用户名、密码、Bucket 或服务器路径。建议变量：

```dotenv
APP_ENV=production
HTTP_ADDR=0.0.0.0:8080
PUBLIC_BASE_URL=https://example.invalid
SITE_TIMEZONE=Asia/Shanghai

DB_HOST=mysql-container-name
DB_PORT=3306
DB_NAME=mywebsite
DB_USER=mywebsite
DB_PASSWORD_FILE=/run/secrets/db_password

UPLOAD_DIR=/data/uploads
MAX_UPLOAD_BYTES=5242880
MAX_IMAGE_PIXELS=20000000
MAX_IMAGE_EDGE=8000

SESSION_IDLE_DURATION=8h
SESSION_ABSOLUTE_DURATION=24h
COOKIE_SECURE=true

GITHUB_API_BASE_URL=https://api.github.com
GITHUB_TIMEOUT=5s
```

真实 `.env` 和 secret 文件位于服务器受限目录，权限只允许部署账号读取。用户选择在本地保留不加密的恢复副本；该副本必须位于项目目录之外，并通过 Windows ACL 限制为当前账号可读。它不等同于加密存储，是已接受风险。

### 13.1 站点资料

网站名称、网名、头像、首页介绍、个人兴趣、邮箱和 GitHub 属于低频站点资料，不进入数据库，也不提供后台编辑。仓库只提交脱敏的 `deploy/site.example.yaml`；实际配置建议保存在 `/opt/mywebsite/config/site.yaml` 并只读挂载到应用容器。

```yaml
site_name: "Example Site"
handle: "example"
tagline: ""
interests: ""
email: "contact@example.invalid"
github_url: "https://github.com/example"
avatar_path: ""
```

应用启动时要求 `site_name`、`handle`、`email` 和 `github_url` 有效。`avatar_path` 缺失时使用内嵌默认头像；`tagline` 或 `interests` 为空时使用产品方案规定的明确占位内容。实际站点资料会在页面公开，但仍与部署 secret 分开管理，以便脱敏发布配置示例。

## 14. 1Panel 与生产网络

目标环境：

- Ubuntu Server 24.04 LTS，`amd64`。
- 至少 4 核 CPU、8 GB 内存。
- 1Panel v1.10.34-lts。
- MySQL Community 8.4.11。
- 已有域名、DNS 控制权和 OpenResty HTTPS 入口。

1Panel 主程序版本与 App Store 应用模板没有可证明的一一历史绑定关系。部署脚本必须先读取实际环境，不得只根据本文猜测容器名或网络。

### 14.1 强制只读预检

```bash
1pctl version

docker ps --format 'table {{.Names}}\t{{.Image}}\t{{.Networks}}\t{{.Ports}}'

docker inspect <openresty-container> \
  --format 'network={{.HostConfig.NetworkMode}} ports={{json .NetworkSettings.Ports}}'

docker inspect <mysql-container> \
  --format 'networkMode={{.HostConfig.NetworkMode}} networks={{json .NetworkSettings.Networks}} ports={{json .HostConfig.PortBindings}}'

docker network inspect 1panel-network

find /opt/1panel/apps/openresty -maxdepth 3 -name docker-compose.yml -print
find /opt/1panel/apps/mysql -maxdepth 3 -name docker-compose.yml -print

docker compose -f <实际-openresty-compose路径> config
docker compose -f <实际-mysql-compose路径> config

ss -lntp
```

预检必须把发现的版本、容器名、网络名、端口绑定和落盘 Compose 路径写入部署记录。任何结果不符合预期时停止，不自动修改 1Panel 管理的 OpenResty 或 MySQL Compose。

### 14.2 预期拓扑

预检确认 OpenResty 使用 host 网络后：

- `app` 发布为 `127.0.0.1:18080:8080`。
- `admin-web` 发布为 `127.0.0.1:18081:8080`。
- OpenResty 反向代理本机回环端口。
- 80 和 443 对公网开放；SSH 只对管理来源开放。预检必须记录当前 1Panel 管理端口和访问方式：优先只允许管理 IP、VPN 或 SSH 隧道访问；如果现状仍需公网管理端口，则先建立白名单再收紧，项目部署不得直接关闭未知的现有入口。

预检确认 MySQL 加入 `1panel-network` 后：

- 只有 `app` 额外加入外部 `1panel-network`。
- 使用实际 MySQL 容器名和容器端口 `3306`。
- 不使用动态容器 IP。
- 不为项目新增 MySQL 公网端口。
- 检查现有 `3306` 绑定、防火墙与云安全组，消除非预期外部访问。

若实际拓扑不同，必须先更新 ADR 和本方案，再改变连接方式。

### 14.3 OpenResty 路由与故障兜底

- `/admin` 永久重定向到 `/admin/`。
- `/admin/*` 代理到管理端静态容器，只有该上游执行 SPA history fallback。
- `/api/v1/*`、`/media/*`、`/-/*` 和其他公开页面代理到 Go。
- API 的真实 404 不得被 SPA fallback 吞掉。
- 在 1Panel 网站目录保存与公开站视觉一致的静态 503 页面和维护标志文件；它们不依赖 `app` 容器。
- 开启上游错误拦截，将连接失败、502、503 和 504 统一映射为静态页面，并把最终 HTTP 状态固定为 503；应用返回的普通 404 保持 404。
- 发布脚本通过原子创建/删除维护标志进入或退出维护模式。具体网站目录必须来自预检结果，不在脚本中猜测。

## 15. Docker Compose

项目 Compose 只包含：

- `app`
- `admin-web`

MySQL、OpenResty 和阿里云 OSS 备份目标都不由项目 Compose 创建。

镜像使用多阶段构建：

- Go 构建阶段生成服务器与 `adminctl`。
- Node 构建阶段生成 Vue 管理端静态文件。
- 公开模板和哈希静态资源通过 `go:embed` 进入 Go 镜像。
- 运行镜像不包含 Go、Node、源码或包管理缓存。

用户已明确选择让运行容器以 root 执行，并且不启用只读根文件系统、`no-new-privileges` 或 capability 收敛。此选择降低初期权限配置成本，但扩大容器利用后的影响范围，属于已知且主动接受的安全风险；不得在验收报告中标记为“容器已加固”。不得挂载 Docker Socket、1Panel 目录或无关宿主机路径。

Docker 日志使用轮转：每文件 10 MB，最多 5 份。上传目录是唯一必须持久化的项目数据挂载。

## 16. 发布与回退

### 16.1 发布前提

- 工作区已经初始化 Git，目标版本具有 Git Tag。
- 单元、集成、前端和关键端到端测试通过。
- 数据库迁移经过独立测试库验证。
- 阿里云 OSS 备份账号、统一批次脚本和 1Panel 目录备份任务可用。
- 服务器磁盘、内存和目标端口满足要求。

### 16.2 发布流程

1. 专用非 root 部署账号获取指定 Git Tag，不直接部署浮动 `main`。
2. 校验 Tag、依赖锁文件、环境文件权限和必要目录。
3. 使用多阶段 Dockerfile 构建带 Tag 的不可变镜像。
4. 在 `127.0.0.1:28080/28081` 启动候选实例，检查镜像版本、`/-/live`、管理静态资源和基础启动行为；候选实例不接收公网流量。
5. 原子创建 OpenResty 维护标志，使公网返回静态 503；等待旧应用在途写请求结束。
6. 生成统一备份批次并通过 1Panel 上传到阿里云 OSS；确认远端对象与清单校验通过。失败时删除维护标志并继续运行旧版本。
7. 停止候选实例和旧稳定容器，确认 `127.0.0.1:18080/18081` 已释放。
8. 显式执行 `goose up`；失败时不启动新版本，按 16.3 恢复旧版本或备份。
9. 使用新镜像启动稳定端口并检查 `/-/ready`。
10. 通过回环地址检查首页、登录页、API 会话、受控图片和管理静态资源。
11. 执行 OpenResty 配置测试；全部通过后删除维护标志并恢复公网流量。
12. 记录 Tag、镜像摘要、迁移版本、备份批次、端口切换和检查结果。

生产服务器是首个实际运行项目容器的环境，但候选镜像必须先在回环临时端口完成检查，不能把第一次启动直接暴露给正式域名。

### 16.3 回退

- 保留上一版本镜像与 Git Tag。
- 数据库迁移优先采用向后兼容的 expand/contract 方式，使上一应用版本仍能读取新 Schema。
- 只有经过验证且不会丢失数据的迁移才允许执行 `goose down`。
- 迁移执行前旧稳定容器已经停止，OpenResty 保持静态 503，因此不存在新旧版本同时写数据库。
- 迁移失败且 Schema 仍与旧版本兼容时，直接重新启动上一镜像、验证就绪后删除维护标志。
- 迁移失败并留下不兼容 Schema 时，先从本次备份恢复数据库，再启动上一镜像；故障回退可能超过正常的 1–2 分钟维护目标。
- 新版本未通过就绪或回环冒烟检查时，不删除维护标志；按相同兼容性判断回退上一镜像。

首版接受每次发布约 1–2 分钟维护窗口，不实现蓝绿部署或多实例滚动更新。

## 17. 备份与恢复

### 17.1 备份集合

项目提供 `deploy/scripts/backup-batch`，先在临时目录生成一个完整批次：

1. 使用实际 MySQL 8.4.11 客户端执行 `mysqldump --single-transaction --skip-lock-tables`，并包含项目需要的触发器等对象。
2. 数据库导出成功后归档 `/opt/mywebsite/data/uploads`。
3. 生成包含批次 ID、Git Tag、Schema 版本、文件大小和 SHA-256 的清单。
4. 全部成功后把临时目录原子移动为 `/opt/mywebsite/backups/ready/current`；失败批次不会替换上一份完整本地批次。

图片文件写入后不再原地修改且首版不自动删除，因此“事务一致的数据库快照在前、图片归档在后”最多带入未被该数据库快照引用的额外图片，不会让快照中的既有引用因并发删除而缺文件。发布前已经进入维护模式，备份则精确对应迁移前状态。

1Panel v1 创建原生“阿里云 OSS”备份账号和目录备份任务，将完整的 `ready/current` 批次上传到 OSS，而不是分别调度两个无法建立依赖关系的数据库/目录任务。日常计划先运行批次生成脚本，只有出现完整成功标记后才允许后续目录备份；发布流程同步等待并确认远端上传。OSS 至少保留 3 个独立批次。

OSS 配置遵守以下约束：

- 使用独立的私有 Bucket，并开启阻止公共访问；备份对象统一放在 `mywebsite/production/backups/` 前缀下。
- 物理服务器使用 Bucket 所在 Region 对应的外网 HTTPS Endpoint；只有服务器确实位于同一阿里云地域和网络时才允许改用内网 Endpoint。1Panel v1 文档没有固定原生 OSS 表单的精确字段，部署预检必须以 v1.10.34-lts 实际表单为准，确认填写的是 Endpoint 还是 Bucket 访问域名。
- 为 1Panel 创建专用 RAM 用户和 AccessKey，使用自定义 RAM Policy，仅允许 `oss:ListObjects` 作用于该 Bucket，以及 `oss:PutObject`、`oss:GetObject` 和按保留规则需要的 `oss:DeleteObject` 作用于上述前缀。若大文件实测触发分片上传，再按实测失败动作补充分片权限；不得直接授予 `oss:*`、使用阿里云主账号 AccessKey 或 `AliyunOSSFullAccess`。
- AccessKey 只保存在 1Panel 备份账号配置中；仓库、环境变量示例、部署记录和日志只出现脱敏占位符。
- 首次启用时实际执行上传、列举、下载、校验和按保留规则删除测试；其中任一步失败都不能把备份任务标记为可用。

脱敏部署记录至少保存以下非秘密参数：

| 配置项 | 记录格式 |
| --- | --- |
| Bucket | `<private-backup-bucket>` |
| Region ID | `<oss-region-id>` |
| 外网 HTTPS Endpoint 或实际表单要求的访问域名 | `<oss-endpoint-or-bucket-domain>` |
| 对象前缀 | `mywebsite/production/backups/` |
| RAM 用户标识 | `<dedicated-backup-ram-user>` |
| 保留规则 | `至少 3 个独立完整批次` |

常规业务备份不包含生产 secret 文件；secret 的本地明文恢复副本由用户单独保管。1Panel 归档使用独立压缩密码，该密码按 secret 管理。OSS 只是异地备份目的地，线上文章和项目图片仍由本地持久卷提供，不直接从 OSS 对外服务。

### 17.2 首次恢复验收

发布第一篇真实文章或第一个真实项目前，必须完成一次隔离恢复：

1. 从阿里云 OSS 下载指定时间点的数据库和图片备份。
2. 在隔离数据库恢复 MySQL dump。
3. 恢复到临时图片目录。
4. 校验备份清单和图片哈希。
5. 使用清单中的同版本应用连接隔离数据。
6. 验证文章正文、稳定 ULID、项目排序、图片引用完整性、公开/私有访问和管理员登录。
7. 记录备份标识、恢复耗时、校验结果和必要修正。

MySQL、1Panel、OSS Region、Endpoint、Bucket 或授权策略发生重大变化后重新执行恢复测试。

## 18. 日志、健康与容量

### 18.1 应用日志

使用 `log/slog` 输出 JSON 到标准输出，至少包含：

- 时间、级别、消息、服务版本。
- 请求 ID、方法、模板化路由、状态码、耗时。
- 管理操作类型和目标 ID，但不记录正文、密码、Cookie、CSRF、数据库密码或完整上传文件。
- 内部错误分类与可追踪错误 ID。

### 18.2 健康检查

- `/-/live` 只证明进程事件循环可响应。
- `/-/ready` 检查 MySQL、当前 Schema 版本、上传目录可写性和必要模板加载。
- GitHub API 不属于就绪依赖，外部故障不能让整个网站退出流量。

### 18.3 日志与磁盘

- Docker 日志：10 MB × 5。
- OpenResty 日志：按天轮转，保留 30 天。
- 每日检查磁盘和上传目录；达到 70% 记录提醒，达到 85% 拒绝新上传但继续提供公开读取。
- 首版不自动删除未引用图片，容量处理由管理员人工决定。

外部 HTTP 与证书监测按用户决定暂不纳入首版；因此服务器完全离线时可能无法主动通知，这是已知延期项。

## 19. 安全边界

### 19.1 HTTP

- 全站 HTTPS；确认稳定后启用 HSTS。
- 使用 CSP 限制为同源脚本、样式、图片和连接，不允许内联脚本。
- 设置 `X-Content-Type-Options: nosniff`、`Referrer-Policy` 和点击劫持防护。
- 只信任来自本机 OpenResty 的转发头；应用端口仅绑定回环地址。
- 管理页面和 API 响应使用 `Cache-Control: no-store`。

### 19.2 数据库

- 使用一个项目专用账号，只在 `mywebsite` 数据库内拥有 DDL/DML 权限。
- 不使用 MySQL `root`，不授予全局管理权限。
- 同一账号同时用于运行和迁移，意味着应用进程运行期间持有改表权限；这是用户为简化首版运维主动接受的风险。
- 数据库错误不直接返回浏览器。

### 19.3 文件

- 禁止路径使用原始文件名。
- 所有路径通过受控 `storage_key` 解析并确认仍位于上传根目录。
- 上传临时目录与最终目录不允许执行文件。
- 不提供目录列表。

### 19.4 外部调用

- GitHub URL 只接受规范的 `https://github.com/{owner}/{repo}` 仓库路径。
- 验证请求使用短连接与约 5 秒超时，不自动跟随到非 GitHub 主机。
- 无 Token 调用遇到限流时返回可重试错误。
- 不因 GitHub 暂时不可用影响已经公开的项目页面。

## 20. 性能与容量基线

首版按以下规模设计：

- 同时在线请求不超过约 100。
- 文章不超过 1000 篇。
- 图片总量控制在 20 GB 内。
- 单个 Go 应用实例。

初始 MySQL 连接池建议从 20 个最大连接、10 个空闲连接开始，根据真实资源调整。HTTP 服务配置读取头、读取请求、写响应和空闲超时，并在收到终止信号后停止接受新请求，最多等待约 30 秒完成在途请求。

公开文章和列表可以使用 ETag/Last-Modified 条件请求；不能缓存管理 API、会话响应或根据管理员身份变化的内容。

## 21. 测试方案

### 21.1 Go 单元测试

- 文章状态转换、首次发布日期和 ULID 不变性。
- 预览、目录、代码高亮和 HTML 禁止规则。
- 图片类型、像素、替代文字和引用可见性。
- 项目 GitHub URL、公开规则和排序。
- 会话空闲/绝对过期、CSRF 和权限检查。
- Problem Details 与稳定错误码。

### 21.2 MySQL 集成测试

- 使用独立的本地 MySQL 8.4 测试数据库，不连接生产。
- 从空库执行全部 `goose up`。
- 验证外键、检查约束、唯一顺序和事务回滚。
- 验证乐观锁冲突不会覆盖新版本。
- 验证文章保存、派生数据和图片引用的原子性。
- 验证发布端点携带未保存表单时只产生一次事务，失败后草稿或公开版本完全不变。
- 验证项目两项互换、全量倒序、集合版本冲突和并发公开不会触发重复顺序或丢失项目。
- 验证迁移失败后不会启动错误 Schema 的应用。

### 21.3 Vue 测试

- 使用 Vitest 与 Testing Library 测试表单校验、错误映射、dirty state、会话恢复和排序。
- 验证 `409`、`401 session_expired`、上传失败和字段错误不会清空输入。
- 验证键盘导航、对话框焦点和主题媒体查询。

### 21.4 端到端测试

使用 Playwright 覆盖至少以下闭环：

1. 登录。
2. 创建只有标题的草稿。
3. 空正文发布失败。
4. 上传图片并插入 Markdown。
5. 发布后获得稳定 ULID 地址。
6. 修改标题后地址不变。
7. 撤回后原地址返回 404，图片不再公开。
8. 创建隐藏项目并公开。
9. GitHub 无效或暂不可验证时显示不同错误。
10. 排序只有明确保存后才改变公开页。
11. 会话过期、同页重新认证、表单仍保留。
12. 多标签页旧版本保存返回冲突。
13. 空内容、404、临时故障、浅色/深色与移动端。
14. 停止 Go 上游后，OpenResty 返回带正确视觉的静态 503，而不是默认 502。
15. `/admin` 规范化跳转、`/admin/*` SPA fallback 和 API 404 互不吞并。

正式验收继续使用原型的 390px、768px 和 1440px 代表宽度，并检查 Chrome、Edge、Firefox 和 Safari 当前版本。

## 22. 实施阶段

### 阶段 0：工程基线

- 初始化 Git，提交当前产品、原型、领域和 ADR 文档。
- 建立单仓库目录、Go 模块、Vue 工程、依赖锁和基础脚本。
- 建立格式化、静态检查、测试和生成命令。
- 定义 OpenAPI、首批迁移和 `sqlc` 配置。

完成证据：Windows 原生环境可以启动 Go、Vue 和本地 MySQL测试；所有生成物可重复生成且工作区无未解释差异。

### 阶段 1：文章发布纵向闭环

- 管理员 CLI、登录、会话和 CSRF。
- 文章草稿、编辑、发布与撤回。
- Markdown 派生、ULID 和公开文章页面。
- 公开文章列表与 404。
- 乐观锁和会话恢复。

完成证据：端到端验证“登录 → 草稿 → 发布 → 公开访问 → 撤回后 404”。

### 阶段 2：图片与项目

- 图片上传、规范化、引用关系和访问控制。
- 项目隐藏、公开、GitHub 验证和人工排序。
- 首页、项目页和关于页统计。

完成证据：上传失败不丢表单；项目排序原子保存；隐藏内容图片不能公开访问。

### 阶段 3：页面与质量

- 把 V2 原型迁移为 Go 模板和 Vue 组件。
- 完成响应式、主题、元信息、错误页和无障碍。
- 完成单元、集成、Vue 和端到端测试矩阵。

完成证据：产品方案 20 个验收场景全部有自动或人工证据。

### 阶段 4：服务器部署

- 完成 1Panel/OpenResty/MySQL 只读预检。
- 创建项目数据库、账号、宿主机目录和 secret。
- 构建候选容器并完成回环冒烟测试。
- 配置 OpenResty、TLS、限速和日志轮转。
- 执行首次正式发布与回退演练。

完成证据：公网空内容站稳定可访问，管理端可以完成全部内容工作流。

### 阶段 5：备份恢复

- 配置统一备份批次脚本与 1Panel 目录备份任务。
- 验证阿里云 OSS 上传、列举、下载、校验、删除权限与至少 3 个时间点保留。
- 在隔离环境完成一次数据库和图片完整恢复。

完成证据：真实内容发布前形成可复查的恢复记录。

## 23. 已接受风险与延期项

| 项目 | 当前决定 | 后续建议 |
| --- | --- | --- |
| 容器权限 | 容器以 root 运行，不增加 capability 或只读根文件系统限制 | 首版稳定后改为非 root 并收敛权限 |
| 数据库权限 | 一个项目账号同时拥有项目库 DDL/DML | 自动化发布成熟后拆分运行与迁移账号 |
| 本地秘密副本 | 项目外明文保存，以 Windows ACL 限制 | 改为密码管理器或加密恢复包 |
| 本地容器验证 | 本地不安装 Docker，生产服务器是首个容器环境 | 后续引入 Docker Desktop、WSL 或 CI 构建 |
| 发布自动化 | 服务器按 Git Tag 手工构建和发布 | 后续由 CI 构建不可变镜像并推送仓库 |
| 外部监测 | 首版暂不配置 | 开始长期发布内容后增加外部 HTTP 与证书监测 |
| 图片清理 | 首版不自动删除孤儿文件 | 未来先做只读报告，再设计人工清理流程 |
| 高可用 | 单机、单实例、允许短维护窗口 | 有真实需求后再评估多实例与对象存储 |
| 数据库锁定 | Schema、驱动和备份只支持 MySQL 8.4，不承诺 MariaDB | 迁移数据库产品必须单独设计和演练 |
| 1Panel 模板 | v1.10.34-lts 无可证明的一一应用模板快照 | 每次部署前以落盘 Compose 和 inspect 为准 |
| 本地图片卷 | 物理服务器磁盘是图片在线服务单点 | 依靠阿里云 OSS 备份恢复，不等同于高可用 |
| ULID 信息 | ULID 会暴露大致生成时间，且不能作为授权凭据 | 只把它当稳定公开标识，权限仍由状态判断 |
| 受控图片性能 | 每次图片请求经过 Go 和数据库引用判断 | 有真实压力后再增加短缓存或派生公开索引 |
| 内容历史 | 只保留当前成功版本，没有内容修订历史 | 依靠备份恢复事故，不提供编辑版本回看 |

这些项目不能在验收时被描述为已经解决。

## 24. 首版技术完成定义

只有同时满足以下条件，技术实现才算首版完成：

- 产品方案中的 20 个验收场景全部通过。
- 公开站和管理端在代表宽度、系统主题和目标浏览器中可用。
- 文章与项目的状态、稳定地址、排序和乐观锁符合数据库约束。
- Markdown、图片和公开访问不存在已知的 HTML/脚本绕过路径。
- 部署使用明确 Git Tag，能够回退上一应用版本。
- 1Panel 实际网络、端口和 MySQL 拓扑已经记录，而不是按模板猜测。
- 生产数据库与图片目录已经进入阿里云 OSS 备份，至少保留 3 个时间点。
- 第一次真实内容发布前已经完成隔离恢复测试。
- 上游完全停止时 OpenResty 能独立返回正确 HTTP 503 和静态故障页。
- 已接受风险与延期项在部署记录中可见。

## 25. 决策索引

- ADR-0001：模块化单体
- ADR-0002：Compose-first 生产部署
- ADR-0003：全站 Vue SPA，已被 ADR-0011 取代
- ADR-0004：MySQL Community 8.4.11
- ADR-0005：复用 1Panel/OpenResty
- ADR-0006：显式 SQL 与 `sqlc`
- ADR-0007：标准库 `net/http`
- ADR-0008：原前端/API 容器边界，已被 ADR-0011 取代
- ADR-0009：服务端管理员会话
- ADR-0010：本地持久卷图片
- ADR-0011：公开站 SSR、管理端 Vue SPA
- ADR-0012：ULID 文章地址
- ADR-0013：显式数据库迁移
- ADR-0014：图片解码与重新编码
- ADR-0015：乐观并发控制
- ADR-0016：按公开引用控制图片访问

## 26. 事实核验来源

以下链接用于核验会随版本变化的基础能力；实施时仍以锁定版本和目标服务器实际输出为准：

- [Go `net/http.ServeMux`](https://go.dev/pkg/net/http/#ServeMux)
- [Vue Quick Start](https://vuejs.org/guide/quick-start)
- [Vite Production Build](https://vite.dev/guide/build)
- [MySQL Release Model](https://dev.mysql.com/doc/refman/9.7/en/mysql-releases.html)
- [1Panel v1.10.34-lts Release](https://github.com/1Panel-dev/1Panel/releases/tag/v1.10.34-lts)
- [1Panel v1 创建反向代理网站](https://1panel.cn/docs/v1/user_manual/websites/website_create/#3)
- [1Panel v1 备份账号](https://1panel.cn/docs/v1/user_manual/settings/#3)
- [1Panel v1 计划任务](https://1panel.cn/docs/v1/user_manual/cronjobs/)
- [阿里云 OSS 地域和 Endpoint](https://www.alibabacloud.com/help/zh/oss/user-guide/regions-and-endpoints)
- [阿里云 OSS RAM Policy](https://help.aliyun.com/zh/oss/user-guide/ram-policy/)
- [阿里云 OSS Bucket 管理与公共访问控制](https://help.aliyun.com/zh/oss/user-guide/oss-bucket-overview)
- [Docker Host Network](https://docs.docker.com/engine/network/drivers/host/)
- [Docker Compose Networking](https://docs.docker.com/compose/how-tos/networking/)
- [Docker Port Publishing](https://docs.docker.com/engine/network/port-publishing/)
