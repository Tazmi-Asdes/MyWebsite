openapi: 3.1.0
info:
  title: MyWebsite Admin API
  version: 0.1.0
  description: 管理端会话、文章、项目和媒体资源 API。
servers:
  - url: /api/v1
    description: 当前站点 API 根路径
tags:
  - name: session
    description: 管理员会话
  - name: articles
    description: 文章管理
  - name: projects
    description: 项目管理
  - name: media
    description: 媒体资源
paths:
  /session:
    post:
      tags: [session]
      operationId: createSession
      summary: 登录
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
      responses:
        '200':
          $ref: '#/components/responses/OK'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
    get:
      tags: [session]
      operationId: getSession
      summary: 读取当前会话
      security:
        - sessionCookie: []
      responses:
        '200':
          $ref: '#/components/responses/OK'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
    delete:
      tags: [session]
      operationId: revokeSession
      summary: 退出登录
      security:
        - sessionCookie: []
      responses:
        '204':
          $ref: '#/components/responses/NoContent'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'

  /session/reauth:
    post:
      tags: [session]
      operationId: reauthenticateSession
      summary: 重新认证并轮换会话
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReauthRequest'
      responses:
        '200':
          $ref: '#/components/responses/OK'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'

  /articles:
    get:
      tags: [articles]
      operationId: listArticles
      summary: 查询文章列表
      description: 支持标题搜索、状态筛选和分页；每页默认 20 篇。
      security:
        - sessionCookie: []
      parameters:
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PerPage'
        - name: q
          in: query
          description: 标题搜索词
          required: false
          schema:
            type: string
            maxLength: 200
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [draft, published]
      responses:
        '200':
          $ref: '#/components/responses/OK'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
    post:
      tags: [articles]
      operationId: createArticle
      summary: 创建草稿
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ArticleWriteRequest'
      responses:
        '201':
          $ref: '#/components/responses/Created'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '413':
          $ref: '#/components/responses/RequestEntityTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'

  /articles/{id}:
    parameters:
      - $ref: '#/components/parameters/ArticleID'
    get:
      tags: [articles]
      operationId: getArticle
      summary: 读取文章编辑数据
      security:
        - sessionCookie: []
      responses:
        '200':
          $ref: '#/components/responses/OK'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
    put:
      tags: [articles]
      operationId: updateArticle
      summary: 保存完整文章编辑状态
      description: 更新和发布均使用记录 version 做乐观锁校验。
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ArticleUpdateRequest'
      responses:
        '200':
          $ref: '#/components/responses/OK'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '413':
          $ref: '#/components/responses/RequestEntityTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'

  /articles/{id}/publish:
    parameters:
      - $ref: '#/components/parameters/ArticleID'
    post:
      tags: [articles]
      operationId: publishArticle
      summary: 原子发布文章
      description: 携带完整编辑状态保存并发布文章。
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ArticleUpdateRequest'
      responses:
        '200':
          $ref: '#/components/responses/OK'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '413':
          $ref: '#/components/responses/RequestEntityTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'

  /articles/{id}/withdraw:
    parameters:
      - $ref: '#/components/parameters/ArticleID'
    post:
      tags: [articles]
      operationId: withdrawArticle
      summary: 撤回文章
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ArticleVersionRequest'
      responses:
        '204':
          $ref: '#/components/responses/NoContent'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'

  /projects:
    get:
      tags: [projects]
      operationId: listProjects
      summary: 查询项目分组
      security:
        - sessionCookie: []
      responses:
        '200':
          $ref: '#/components/responses/OK'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
    post:
      tags: [projects]
      operationId: createProject
      summary: 创建隐藏项目
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProjectWriteRequest'
      responses:
        '201':
          $ref: '#/components/responses/Created'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'

  /projects/{id}:
    parameters:
      - $ref: '#/components/parameters/ProjectID'
    get:
      tags: [projects]
      operationId: getProject
      summary: 读取项目编辑数据
      security:
        - sessionCookie: []
      responses:
        '200':
          $ref: '#/components/responses/OK'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
    put:
      tags: [projects]
      operationId: updateProject
      summary: 保存完整项目编辑状态
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProjectWriteRequest'
      responses:
        '200':
          $ref: '#/components/responses/OK'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'

  /projects/{id}/publish:
    parameters:
      - $ref: '#/components/parameters/ProjectID'
    post:
      tags: [projects]
      operationId: publishProject
      summary: 原子公开项目
      description: 携带完整项目表单并在排序集合锁内追加公开项目。
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProjectWriteRequest'
      responses:
        '200':
          $ref: '#/components/responses/OK'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'

  /projects/{id}/hide:
    parameters:
      - $ref: '#/components/parameters/ProjectID'
    post:
      tags: [projects]
      operationId: hideProject
      summary: 隐藏项目
      security:
        - sessionCookie: []
      responses:
        '204':
          $ref: '#/components/responses/NoContent'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'

  /projects/order:
    put:
      tags: [projects]
      operationId: updateProjectOrder
      summary: 原子保存公开项目顺序
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProjectOrderRequest'
      responses:
        '200':
          $ref: '#/components/responses/OK'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'

  /media:
    post:
      tags: [media]
      operationId: uploadMedia
      summary: 上传媒体资源
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
                  description: 图片文件。
      responses:
        '201':
          $ref: '#/components/responses/Created'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '413':
          $ref: '#/components/responses/RequestEntityTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'

  /media/{id}:
    parameters:
      - $ref: '#/components/parameters/MediaID'
    get:
      tags: [media]
      operationId: getMedia
      summary: 读取媒体资源
      security:
        - sessionCookie: []
      responses:
        '200':
          $ref: '#/components/responses/OK'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'

components:
  securitySchemes:
    sessionCookie:
      type: apiKey
      in: cookie
      name: session

  parameters:
    ArticleID:
      name: id
      in: path
      required: true
      description: 文章内部 ID。
      schema:
        type: integer
        format: int64
        minimum: 1
    ProjectID:
      name: id
      in: path
      required: true
      description: 项目内部 ID。
      schema:
        type: integer
        format: int64
        minimum: 1
    MediaID:
      name: id
      in: path
      required: true
      description: 媒体资源公开 ULID。
      schema:
        $ref: '#/components/schemas/ULID'
    Page:
      name: page
      in: query
      required: false
      schema:
        type: integer
        format: int32
        minimum: 1
        default: 1
    PerPage:
      name: per_page
      in: query
      required: false
      schema:
        type: integer
        format: int32
        minimum: 1
        maximum: 20
        default: 20

  responses:
    OK:
      description: 请求成功。
      content:
        application/json:
          schema:
            oneOf:
              - $ref: '#/components/schemas/Session'
              - $ref: '#/components/schemas/ArticleListResponse'
              - $ref: '#/components/schemas/ArticleEdit'
              - $ref: '#/components/schemas/ProjectGroups'
              - $ref: '#/components/schemas/ProjectEdit'
              - $ref: '#/components/schemas/MediaAsset'
    Created:
      description: 资源创建成功。
      content:
        application/json:
          schema:
            oneOf:
              - $ref: '#/components/schemas/ArticleEdit'
              - $ref: '#/components/schemas/ProjectEdit'
              - $ref: '#/components/schemas/MediaAsset'
    NoContent:
      description: 请求成功，无响应正文。
    BadRequest:
      description: 请求语法错误。
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/ProblemDetails'
    Unauthorized:
      description: 未登录、凭据无效或会话已过期。
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/ProblemDetails'
    Forbidden:
      description: 已登录但没有权限或 CSRF 校验失败。
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/ProblemDetails'
    NotFound:
      description: 资源不存在。
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/ProblemDetails'
    Conflict:
      description: 版本或排序集合版本冲突。
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/ProblemDetails'
    RequestEntityTooLarge:
      description: 请求体或上传文件过大。
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/ProblemDetails'
    UnsupportedMediaType:
      description: 不支持的请求媒体类型。
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/ProblemDetails'
    UnprocessableEntity:
      description: 字段或业务校验失败。
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/ProblemDetails'
    TooManyRequests:
      description: 请求过于频繁。
      headers:
        Retry-After:
          schema:
            type: integer
            minimum: 0
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/ProblemDetails'
    InternalServerError:
      description: 服务器内部错误。
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/ProblemDetails'
    ServiceUnavailable:
      description: 数据库或必要依赖暂时不可用。
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/ProblemDetails'

  schemas:
    ULID:
      type: string
      minLength: 26
      maxLength: 26
      pattern: '^[0-9A-HJKMNP-TV-Z]{26}$'
      description: Crockford Base32 编码的公开 ULID。
    ProblemDetails:
      type: object
      required: [type, title, status, code, request_id, errors]
      properties:
        type:
          type: string
          format: uri-reference
        title:
          type: string
        status:
          type: integer
          format: int32
          minimum: 400
          maximum: 599
        code:
          type: string
          description: 稳定机器可读错误码。
          enum:
            - validation_failed
            - authentication_required
            - session_expired
            - csrf_failed
            - version_conflict
            - github_repository_invalid
            - github_verification_unavailable
            - upload_too_large
            - upload_type_invalid
            - image_dimensions_exceeded
            - save_failed
            - invalid_request
            - not_found
            - unsupported_media_type
            - rate_limited
            - dependency_unavailable
        request_id:
          type: string
        errors:
          type: object
          additionalProperties:
            type: array
            items:
              type: string
    LoginRequest:
      type: object
      required: [username, password]
      additionalProperties: false
      properties:
        username:
          type: string
          minLength: 3
          maxLength: 64
        password:
          type: string
          format: password
          minLength: 1
          maxLength: 1024
    ReauthRequest:
      type: object
      required: [password]
      additionalProperties: false
      properties:
        password:
          type: string
          format: password
          minLength: 1
          maxLength: 1024
    Session:
      type: object
      required: [admin_id, username, csrf_token, last_seen_at, expires_at, absolute_expires_at]
      properties:
        admin_id:
          type: integer
          format: int64
          minimum: 1
        username:
          type: string
        csrf_token:
          type: string
        last_seen_at:
          type: string
          format: date-time
        expires_at:
          type: string
          format: date-time
        absolute_expires_at:
          type: string
          format: date-time
    ArticleSummary:
      type: object
      required: [id, title, status, version, created_at, updated_at]
      properties:
        id:
          type: integer
          format: int64
          minimum: 1
        public_ulid:
          allOf:
            - $ref: '#/components/schemas/ULID'
          nullable: true
        title:
          type: string
        status:
          type: string
          enum: [draft, published]
        first_published_at:
          type: string
          format: date-time
          nullable: true
        version:
          type: integer
          format: int64
          minimum: 1
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ArticleEdit:
      type: object
      required: [id, title, status, version, created_at, updated_at]
      properties:
        id:
          type: integer
          format: int64
          minimum: 1
        public_ulid:
          allOf:
            - $ref: '#/components/schemas/ULID'
          nullable: true
        title:
          type: string
          minLength: 1
          maxLength: 200
        body_markdown:
          type: string
          nullable: true
          maxLength: 2097152
        body_html:
          type: string
          nullable: true
          readOnly: true
        toc_json:
          type: object
          readOnly: true
        preview_text:
          type: string
          nullable: true
          readOnly: true
        renderer_version:
          type: string
          nullable: true
          readOnly: true
        status:
          type: string
          enum: [draft, published]
        first_published_at:
          type: string
          format: date-time
          nullable: true
          readOnly: true
        version:
          type: integer
          format: int64
          minimum: 1
        created_at:
          type: string
          format: date-time
          readOnly: true
        updated_at:
          type: string
          format: date-time
          readOnly: true
    ArticleWriteRequest:
      type: object
      required: [title, body_markdown]
      additionalProperties: false
      properties:
        title:
          type: string
          minLength: 1
          maxLength: 200
        body_markdown:
          type: string
          nullable: true
          maxLength: 2097152
          description: 草稿可为空；发布时必须为非空正文。
        version:
          type: integer
          format: int64
          minimum: 1
          description: 更新、发布和撤回时必填的乐观锁版本。
    ArticleUpdateRequest:
      allOf:
        - $ref: '#/components/schemas/ArticleWriteRequest'
        - type: object
          required: [version]
          properties:
            version:
              type: integer
              format: int64
              minimum: 1
    ArticleVersionRequest:
      type: object
      required: [version]
      additionalProperties: false
      properties:
        version:
          type: integer
          format: int64
          minimum: 1
    ProjectEdit:
      type: object
      required: [id, name, status, version, created_at, updated_at]
      properties:
        id:
          type: integer
          format: int64
          minimum: 1
        name:
          type: string
          minLength: 1
          maxLength: 120
        github_url:
          type: string
          format: uri
          maxLength: 2048
          nullable: true
        image_asset_id:
          allOf:
            - $ref: '#/components/schemas/ULID'
          nullable: true
        status:
          type: string
          enum: [hidden, public]
        sort_order:
          type: integer
          format: int64
          minimum: 1
          nullable: true
        version:
          type: integer
          format: int64
          minimum: 1
        created_at:
          type: string
          format: date-time
          readOnly: true
        updated_at:
          type: string
          format: date-time
          readOnly: true
    ProjectWriteRequest:
      type: object
      required: [name]
      additionalProperties: false
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 120
        github_url:
          type: string
          format: uri
          maxLength: 2048
          nullable: true
        image_asset_id:
          allOf:
            - $ref: '#/components/schemas/ULID'
          nullable: true
        version:
          type: integer
          format: int64
          minimum: 1
          description: 更新、公开和隐藏时必填的乐观锁版本。
    ProjectGroups:
      type: object
      required: [public, hidden, order_version]
      properties:
        public:
          type: array
          items:
            $ref: '#/components/schemas/ProjectEdit'
        hidden:
          type: array
          items:
            $ref: '#/components/schemas/ProjectEdit'
        order_version:
          type: integer
          format: int64
          minimum: 1
    ProjectOrderRequest:
      type: object
      required: [order_version, project_ids]
      additionalProperties: false
      properties:
        order_version:
          type: integer
          format: int64
          minimum: 1
        project_ids:
          type: array
          uniqueItems: true
          items:
            type: integer
            format: int64
            minimum: 1
    MediaAsset:
      type: object
      required: [id, preview_url, markdown_reference, source_media_type, stored_media_type, byte_size, width, height, created_at]
      properties:
        id:
          $ref: '#/components/schemas/ULID'
        preview_url:
          type: string
          format: uri-reference
        markdown_reference:
          type: string
        source_media_type:
          type: string
        stored_media_type:
          type: string
        byte_size:
          type: integer
          format: int64
          minimum: 0
        width:
          type: integer
          format: int32
          minimum: 0
        height:
          type: integer
          format: int32
          minimum: 0
        sha256:
          type: string
          pattern: '^[0-9a-fA-F]{64}$'
        created_at:
          type: string
          format: date-time
    PaginationMeta:
      type: object
      required: [page, per_page, total, total_pages]
      properties:
        page:
          type: integer
          format: int32
          minimum: 1
        per_page:
          type: integer
          format: int32
          minimum: 1
        total:
          type: integer
          format: int64
          minimum: 0
        total_pages:
          type: integer
          format: int32
          minimum: 0
    ArticleListResponse:
      type: object
      required: [items, pagination]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/ArticleSummary'
        pagination:
          $ref: '#/components/schemas/PaginationMeta'
